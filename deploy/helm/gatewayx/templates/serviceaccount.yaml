apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gatewayx.serviceAccountName" . }}
  labels:
    {{- include "gatewayx.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
